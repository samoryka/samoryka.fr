export interface ProjectItem {
    title: string;
    description: string;
    link: string;
    image: string;
    technologies: string[];
};

export interface HeaderItem {
    link: string;
    title: string;
};